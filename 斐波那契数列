#include<iostream>

using namespace std;

int Fibonacci(int n,int *F)
{
	
	if (n == 1 || n == 0)
	{
		F[n] = 1;
		return F[n];
	}
	else
	{
		F[n]= Fibonacci(n - 1,F) + Fibonacci(n - 2,F);
		return F[n];
	}
}

int main()
{
	int n = 0;
	cin >> n;
	int *F = new int[n + 1];
	Fibonacci(n,F);
	for (int i = 0; i <= n; i++)
	{
		cout << F[i] << " ";
	}
	return 0;
}
